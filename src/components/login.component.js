import React from 'react';

const LoginForm = () => {
    return (
        <React.Fragment>
            <h3>Have an account ? </h3>
            <h5>Login here</h5>
        </React.Fragment>
    );
};

export default LoginForm;